[package]
name = "portablenote-infra"
version = "0.1.0"
edition = "2021"

[dependencies]
portablenote-core = { path = "../core" }
serde_json = "1"
uuid = { version = "1", features = ["v4", "serde"] }

[dev-dependencies]
chrono = "0.4"
tempfile = "3"
